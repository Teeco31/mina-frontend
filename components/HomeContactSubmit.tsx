'use client'

export default function HomeContactSubmit() {
  return (
    <button
      type="button"
      onClick={() => {}}
      className="self-start px-10 py-3.5 bg-gold text-white text-[11px] tracking-[0.2em] uppercase hover:bg-gold-light transition-colors"
    >
      Send Message
    </button>
  )
}
